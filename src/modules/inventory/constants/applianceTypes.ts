/**
 * Legacy appliance type grid — re-exported from `itemCategories` during migration.
 * Prefer `ITEM_CATEGORIES`, `CategoryPicker`, and typed `item_category` columns.
 */
export { APPLIANCE_TYPES, type ApplianceTypeOption } from "./itemCategories"
