# Emulator param values (demo-homehub). Empty = identity layer dormant.
# Without this file the emulator PROMPTS interactively for every defineString
# param and hangs headless runs. Real keys never go here — this file is
# committed; prod values live in .env.homehub-2068d (gitignored).
ICECAT_USERNAME=
BRAVE_SEARCH_API_KEY=
